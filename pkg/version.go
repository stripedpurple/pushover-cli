package pkg

const VersionShort = "1.0"
const VersionLong = VersionShort + " " + "Pilot" // Each version bump should be the next episode in M*A*S*H
const ApplicationName = "Pushover CLI"
const ApplicationFullName = "Pushover CLI v" + VersionLong
const DescriptionShort = "Simple to use pushover command line interface."
const DescriptionLong = ApplicationFullName + "\n" + `A longer description that spans multiple lines and likely contains
examples and usage of using your application. For example:

Cobra is a CLI library for Go that empowers applications.
This application is a tool to generate the needed files
to quickly create a Cobra application.`
